#TODO here: __all__ = ["main"], instantiate app
#then in the highest level dir of backend, add a run.py to simply
#catch the __name__ == __main__ scope and run the create_app() result
#with run() and init_db if needed or 'with context man' app_context()
#on the appropriate host and port for the server launched
#for developement at least